version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  build:
    commands:
      - cd frontend_web/client
      - npm install
      - npm run build
      - cd ..
      - npm install
      - npm run build
artifacts:
  files:
    - "./**/**"
  base-directory: "frontend_web"
